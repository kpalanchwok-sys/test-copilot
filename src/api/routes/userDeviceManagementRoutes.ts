// import express from "express";
// import {
//   createUserDevice,
//   getAllUserDevices,
// } from "../controllers/userDeviceManagementController";

// const router = express.Router();

// router.post("/", createUserDevice);
// router.get("/", getAllUserDevices);
// // router.get("/:id", getSingleUserDevice);
// // router.put("/:id", updateUserDevice);
// // router.delete("/:id", deleteUserDevice);

// export default router;
